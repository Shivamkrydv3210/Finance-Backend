# Optional repo-root .env — invoice-backend loads this (see invoice-backend/src/config.js)

PORT=3001
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
OPENAI_API_KEY=sk-...

# Optional: apply schema from CLI (cd invoice-backend && npm run db:apply-schema)
# Supabase → Settings → Database → use your database password (not the service_role JWT).
# Direct (port 5432), this project ref hyvfsfiduysfmkllghdg:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.hyvfsfiduysfmkllghdg.supabase.co:5432/postgres?sslmode=require
