# Optional: copy to invoice-backend/.env for local overrides only (e.g. PORT).
# Put Supabase + OpenAI secrets in repo-root .env — see README / src/config.js.

PORT=3001

# Only if you are NOT using repo-root .env:
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# OPENAI_API_KEY=sk-...
